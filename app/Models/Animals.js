export class Alligator {
  constructor(name, color, gender, weight, tailLengthInch, teethSize, animalNoise, animalFood) {
    this.name = name;
    this.color = color;
    this.gender = gender;
    this.weight = weight;
    this.tailLengthInch = tailLengthInch;
    this.teethSize = teethSize;
    this.animalNoise = animalNoise;
    this.animalFood = animalFood;
  }

  eat(animalFood) {
    console.log(`nom nom yummy ${animalFood}.`)
  }
  speak(animalNoise) {
    alert(`The ${this.constructor.name} says ${animalNoise}!`)
  }
}

export class Bullfrog {
  constructor(name, color, gender, weight, tailLengthInch, teethSize, animalNoise, animalFood) {
    this.name = name;
    this.color = color;
    this.gender = gender;
    this.weight = weight;
    this.tailLengthInch = tailLengthInch;
    this.teethSize = teethSize;
    this.animalNoise = animalNoise;
    this.animalFood = animalFood;
  }

  eat(animalFood) {
    console.log(`nom nom yummy ${animalFood}.`)
  }
  speak(animalNoise) {
    alert(`The ${this.constructor.name} says ${animalNoise}!`)
  }
}

export class Cheetah {
  constructor(name, color, gender, weight, tailLengthInch, teethSize, animalNoise, animalFood) {
    this.name = name;
    this.color = color;
    this.gender = gender;
    this.weight = weight;
    this.tailLengthInch = tailLengthInch;
    this.teethSize = teethSize;
    this.animalNoise = animalNoise;
    this.animalFood = animalFood;
  }

  eat(animalFood) {
    console.log(`nom nom yummy ${animalFood}.`)
  }
  speak(animalNoise) {
    alert(`The ${this.constructor.name} says ${animalNoise}!`)
  }
}

export class Dragon {
  constructor(name, color, gender, weight, tailLengthInch, teethSize, animalNoise, animalFood) {
    this.name = name;
    this.color = color;
    this.gender = gender;
    this.weight = weight;
    this.tailLengthInch = tailLengthInch;
    this.teethSize = teethSize;
    this.animalNoise = animalNoise;
    this.animalFood = animalFood;
  }

  eat(animalFood) {
    console.log(`nom nom yummy ${animalFood}.`)
  }
  speak(animalNoise) {
    alert(`The ${this.constructor.name} says ${animalNoise}!`)
  }
}

export class Elephant {
  constructor(name, color, gender, weight, tailLengthInch, teethSize, animalNoise, animalFood) {
    this.name = name;
    this.color = color;
    this.gender = gender;
    this.weight = weight;
    this.tailLengthInch = tailLengthInch;
    this.teethSize = teethSize;
    this.animalNoise = animalNoise;
    this.animalFood = animalFood;
  }

  eat(animalFood) {
    console.log(`nom nom yummy ${animalFood}.`)
  }
  speak(animalNoise) {
    alert(`The ${this.constructor.name} says ${animalNoise}!`)
  }
}

export class Falcon {
  constructor(name, color, gender, weight, tailLengthInch, teethSize, animalNoise, animalFood) {
    this.name = name;
    this.color = color;
    this.gender = gender;
    this.weight = weight;
    this.tailLengthInch = tailLengthInch;
    this.teethSize = teethSize;
    this.animalNoise = animalNoise;
    this.animalFood = animalFood;
  }

  eat(animalFood) {
    console.log(`nom nom yummy ${animalFood}.`)
  }
  speak(animalNoise) {
    alert(`The ${this.constructor.name} says ${animalNoise}!`)
  }
}

export class Gazelle {
  constructor(name, color, gender, weight, tailLengthInch, teethSize, animalNoise, animalFood) {
    this.name = name;
    this.color = color;
    this.gender = gender;
    this.weight = weight;
    this.tailLengthInch = tailLengthInch;
    this.teethSize = teethSize;
    this.animalNoise = animalNoise;
    this.animalFood = animalFood;
  }

  eat(animalFood) {
    console.log(`nom nom yummy ${animalFood}.`)
  }
  speak(animalNoise) {
    alert(`The ${this.constructor.name} says ${animalNoise}!`)
  }
}

export class Hippopotamus {
  constructor(name, color, gender, weight, tailLengthInch, teethSize, animalNoise, animalFood) {
    this.name = name;
    this.color = color;
    this.gender = gender;
    this.weight = weight;
    this.tailLengthInch = tailLengthInch;
    this.teethSize = teethSize;
    this.animalNoise = animalNoise;
    this.animalFood = animalFood;
  }

  eat(animalFood) {
    console.log(`nom nom yummy ${animalFood}.`)
  }
  speak(animalNoise) {
    alert(`The ${this.constructor.name} says ${animalNoise}!`)
  }
}

export class Iguana {
  constructor(name, color, gender, weight, tailLengthInch, teethSize, animalNoise, animalFood) {
    this.name = name;
    this.color = color;
    this.gender = gender;
    this.weight = weight;
    this.tailLengthInch = tailLengthInch;
    this.teethSize = teethSize;
    this.animalNoise = animalNoise;
    this.animalFood = animalFood;
  }

  eat(animalFood) {
    console.log(`nom nom yummy ${animalFood}.`)
  }
  speak(animalNoise) {
    alert(`The ${this.constructor.name} says ${animalNoise}!`)
  }
}

export class Jackal {
  constructor(name, color, gender, weight, tailLengthInch, teethSize, animalNoise, animalFood) {
    this.name = name;
    this.color = color;
    this.gender = gender;
    this.weight = weight;
    this.tailLengthInch = tailLengthInch;
    this.teethSize = teethSize;
    this.animalNoise = animalNoise;
    this.animalFood = animalFood;
  }

  eat(animalFood) {
    console.log(`nom nom yummy ${animalFood}.`)
  }
  speak(animalNoise) {
    alert(`The ${this.constructor.name} says ${animalNoise}!`)
  }
}

export class Kangaroo {
  constructor(name, color, gender, weight, tailLengthInch, teethSize, animalNoise, animalFood) {
    this.name = name;
    this.color = color;
    this.gender = gender;
    this.weight = weight;
    this.tailLengthInch = tailLengthInch;
    this.teethSize = teethSize;
    this.animalNoise = animalNoise;
    this.animalFood = animalFood;
  }

  eat(animalFood) {
    console.log(`nom nom yummy ${animalFood}.`)
  }
  speak(animalNoise) {
    alert(`The ${this.constructor.name} says ${animalNoise}!`)
  }
}

export class Leopard {
  constructor(name, color, gender, weight, tailLengthInch, teethSize, animalNoise, animalFood) {
    this.name = name;
    this.color = color;
    this.gender = gender;
    this.weight = weight;
    this.tailLengthInch = tailLengthInch;
    this.teethSize = teethSize;
    this.animalNoise = animalNoise;
    this.animalFood = animalFood;
  }

  eat(animalFood) {
    console.log(`nom nom yummy ${animalFood}.`)
  }
  speak(animalNoise) {
    alert(`The ${this.constructor.name} says ${animalNoise}!`)
  }
}

export class Magpie {
  constructor(name, color, gender, weight, tailLengthInch, teethSize, animalNoise, animalFood) {
    this.name = name;
    this.color = color;
    this.gender = gender;
    this.weight = weight;
    this.tailLengthInch = tailLengthInch;
    this.teethSize = teethSize;
    this.animalNoise = animalNoise;
    this.animalFood = animalFood;
  }

  eat(animalFood) {
    console.log(`nom nom yummy ${animalFood}.`)
  }
  speak(animalNoise) {
    alert(`The ${this.constructor.name} says ${animalNoise}!`)
  }
}

export class Narwal {
  constructor(name, color, gender, weight, tailLengthInch, teethSize, animalNoise, animalFood) {
    this.name = name;
    this.color = color;
    this.gender = gender;
    this.weight = weight;
    this.tailLengthInch = tailLengthInch;
    this.teethSize = teethSize;
    this.animalNoise = animalNoise;
    this.animalFood = animalFood;
  }

  eat(animalFood) {
    console.log(`nom nom yummy ${animalFood}.`)
  }
  speak(animalNoise) {
    alert(`The ${this.constructor.name} says ${animalNoise}!`)
  }
}

export class Ocelot {
  constructor(name, color, gender, weight, tailLengthInch, teethSize, animalNoise, animalFood) {
    this.name = name;
    this.color = color;
    this.gender = gender;
    this.weight = weight;
    this.tailLengthInch = tailLengthInch;
    this.teethSize = teethSize;
    this.animalNoise = animalNoise;
    this.animalFood = animalFood;
  }

  eat(animalFood) {
    console.log(`nom nom yummy ${animalFood}.`)
  }
  speak(animalNoise) {
    alert(`The ${this.constructor.name} says ${animalNoise}!`)
  }
}

export class Pirahana {
  constructor(name, color, gender, weight, tailLengthInch, teethSize, animalNoise, animalFood) {
    this.name = name;
    this.color = color;
    this.gender = gender;
    this.weight = weight;
    this.tailLengthInch = tailLengthInch;
    this.teethSize = teethSize;
    this.animalNoise = animalNoise;
    this.animalFood = animalFood;
  }

  eat(animalFood) {
    console.log(`nom nom yummy ${animalFood}.`)
  }
  speak(animalNoise) {
    alert(`The ${this.constructor.name} says ${animalNoise}!`)
  }
}

export class Quail {
  constructor(name, color, gender, weight, tailLengthInch, teethSize, animalNoise, animalFood) {
    this.name = name;
    this.color = color;
    this.gender = gender;
    this.weight = weight;
    this.tailLengthInch = tailLengthInch;
    this.teethSize = teethSize;
    this.animalNoise = animalNoise;
    this.animalFood = animalFood;
  }

  eat(animalFood) {
    console.log(`nom nom yummy ${animalFood}.`)
  }
  speak(animalNoise) {
    alert(`The ${this.constructor.name} says ${animalNoise}!`)
  }
}

export class Rhino {
  constructor(name, color, gender, weight, tailLengthInch, teethSize, animalNoise, animalFood) {
    this.name = name;
    this.color = color;
    this.gender = gender;
    this.weight = weight;
    this.tailLengthInch = tailLengthInch;
    this.teethSize = teethSize;
    this.animalNoise = animalNoise;
    this.animalFood = animalFood;
  }

  eat(animalFood) {
    console.log(`nom nom yummy ${animalFood}.`)
  }
  speak(animalNoise) {
    alert(`The ${this.constructor.name} says ${animalNoise}!`)
  }
}

export class Snake {
  constructor(name, color, gender, weight, tailLengthInch, teethSize, animalNoise, animalFood) {
    this.name = name;
    this.color = color;
    this.gender = gender;
    this.weight = weight;
    this.tailLengthInch = tailLengthInch;
    this.teethSize = teethSize;
    this.animalNoise = animalNoise;
    this.animalFood = animalFood;
  }

  eat(animalFood) {
    console.log(`nom nom yummy ${animalFood}.`)
  }
  speak(animalNoise) {
    alert(`The ${this.constructor.name} says ${animalNoise}!`)
  }
}

export class Triceritops {
  constructor(name, color, gender, weight, tailLengthInch, teethSize, animalNoise, animalFood) {
    this.name = name;
    this.color = color;
    this.gender = gender;
    this.weight = weight;
    this.tailLengthInch = tailLengthInch;
    this.teethSize = teethSize;
    this.animalNoise = animalNoise;
    this.animalFood = animalFood;
  }

  eat(animalFood) {
    console.log(`nom nom yummy ${animalFood}.`)
  }
  speak(animalNoise) {
    alert(`The ${this.constructor.name} says ${animalNoise}!`)
  }
}

export class Unicorn {
  constructor(name, color, gender, weight, tailLengthInch, teethSize, animalNoise, animalFood) {
    this.name = name;
    this.color = color;
    this.gender = gender;
    this.weight = weight;
    this.tailLengthInch = tailLengthInch;
    this.teethSize = teethSize;
    this.animalNoise = animalNoise;
    this.animalFood = animalFood;
  }

  eat(animalFood) {
    console.log(`nom nom yummy ${animalFood}.`)
  }
  speak(animalNoise) {
    alert(`The ${this.constructor.name} says ${animalNoise}!`)
  }
}

export class Velociraptor {
  constructor(name, color, gender, weight, tailLengthInch, teethSize, animalNoise, animalFood) {
    this.name = name;
    this.color = color;
    this.gender = gender;
    this.weight = weight;
    this.tailLengthInch = tailLengthInch;
    this.teethSize = teethSize;
    this.animalNoise = animalNoise;
    this.animalFood = animalFood;
  }

  eat(animalFood) {
    console.log(`nom nom yummy ${animalFood}.`)
  }
  speak(animalNoise) {
    alert(`The ${this.constructor.name} says ${animalNoise}!`)
  }
}

export class Whale {
  constructor(name, color, gender, weight, tailLengthInch, teethSize, animalNoise, animalFood) {
    this.name = name;
    this.color = color;
    this.gender = gender;
    this.weight = weight;
    this.tailLengthInch = tailLengthInch;
    this.teethSize = teethSize;
    this.animalNoise = animalNoise;
    this.animalFood = animalFood;
  }

  eat() {
    console.log(`nom nom yummy ${this.constructor.animalFood}.`)
  }
  speak() {
    alert(`The ${this.constructor.name} says ${this.constructor.animalNoise}!`)
  }
  get animalTemplate() {
    return `<p>${this.constructor.name} the ${this.constructor.canFly ? 'flying' : ''}${this.constructor.type}, says hello.</p>`
  }
}

export class Xerus {
  constructor(name, color, gender, weight, tailLengthInch, teethSize, animalNoise, animalFood) {
    this.name = name;
    this.color = color;
    this.gender = gender;
    this.weight = weight;
    this.tailLengthInch = tailLengthInch;
    this.teethSize = teethSize;
    this.animalNoise = animalNoise;
    this.animalFood = animalFood;
  }

  eat(animalFood) {
    console.log(`nom nom yummy ${animalFood}.`)
  }
  speak(animalNoise) {
    alert(`The ${this.constructor.name} says ${animalNoise}!`)
  }
}

export class Yak {
  constructor(name, color, gender, weight, tailLengthInch, teethSize, animalNoise, animalFood) {
    this.name = name;
    this.color = color;
    this.gender = gender;
    this.weight = weight;
    this.tailLengthInch = tailLengthInch;
    this.teethSize = teethSize;
    this.animalNoise = animalNoise;
    this.animalFood = animalFood;
  }

  eat(animalFood) {
    console.log(`nom nom yummy ${animalFood}.`)
  }
  speak(animalNoise) {
    alert(`The ${this.constructor.name} says ${animalNoise}!`)
  }
}

export class Zebra {
  constructor(name, color, gender, weight, tailLengthInch, teethSize, animalNoise, animalFood) {
    this.name = name;
    this.color = color;
    this.gender = gender;
    this.weight = weight;
    this.tailLengthInch = tailLengthInch;
    this.teethSize = teethSize;
    this.animalNoise = animalNoise;
    this.animalFood = animalFood;
  }

  eat(animalFood) {
    console.log(`nom nom yummy ${animalFood}.`)
  }
  speak(animalNoise) {
    alert(`The ${this.constructor.name} says ${animalNoise}!`)
  }
}








